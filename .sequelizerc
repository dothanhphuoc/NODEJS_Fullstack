var path = require('path')

module.exports = {
    'config': path.resolve('./src/configs', 'config.json'),
    'migrations-path': path.resolve('./src', 'migrations'), //cách tạo các bảng trong database mà không cần dùng câu lệnh SQL
    'models-path': path.resolve('./src', 'models'),
    'seeders-path': path.resolve('./src', 'seeders'), //tạo dữ liệu fake
}